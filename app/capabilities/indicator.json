{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "indicator",
  "description": "The recording pill only listens for its state; it controls no windows.",
  "windows": ["hud"],
  "permissions": ["core:event:default"]
}
